{
  "name": "jobpilot",
  "version": "0.5.0",
  "private": true,
  "description": "JobPilot Android-first application with its own local-first backend and phone-first Web client",
  "license": "AGPL-3.0-only",
  "engines": { "node": ">=22.0.0" },
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix web run start",
    "test": "npm --prefix web test",
    "typecheck": "npm --prefix web run typecheck",
    "qa:backend": "npm --prefix web run qa:backend"
  },
  "repository": { "type": "git", "url": "https://github.com/happyivanencoding/JPilot" }
}
